# Calculus Flow legacy artifacts

The editable Calculus Flow project now lives at `apps/calculus-flow/`.

This legacy staging directory is intentionally retained for its existing generated `dist/`, tracked `dist-new/`, local `node_modules/`, and other staging artifacts. Do not make source changes here. Use the repository-root `calc-flow:*` commands documented in `apps/calculus-flow/README.md`.
